apiVersion: v1
kind: Service
metadata:
  name: todo-backend-service
  namespace: todolist
spec:
  type: NodePort
  selector:
    app: todo-backend
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30174
